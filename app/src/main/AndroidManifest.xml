<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a2170009.club_entry_exit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_setting"
            android:label="@string/name_setting">
        </activity>
        <activity android:name=".EnteringActivity"
            android:label="@string/entryExit_label"></activity>
        <activity android:name=".room_detailsActivity"></activity>
        <activity android:name=".room_situationActivity"></activity>
        <activity android:name=".showActivity"></activity>
<!--        <activity android:name=".room_situationActivity"></activity>-->
    </application>

</manifest>